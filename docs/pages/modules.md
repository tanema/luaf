# Lua Modules and Structure
Here are some notes about how modules are thought about in lua and what the terminology 
actually is.

- Package is a file with lua code in it that can be required
- Module is a required file lua functionality, though with ldoc, many modules can 
be contained in a package.
- What is a directory with an `init.lua` file? Does that not make the whole directory a package?
